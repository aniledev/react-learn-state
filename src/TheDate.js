import React from "react";

class TheDate extends React.Component {
  constructor(props) {
    super(props);
    // state is usually set as an object; think of state as the STORE data in earlier projects
    this.state = {};
  }
  render() {
    return <div></div>;
  }
}

export default TheDate;
